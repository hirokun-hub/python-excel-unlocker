name: 🚦 CI (AI-first, lightweight)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ai-artifacts:
    name: 🧭 Collect AI Artifacts
    uses: ./.github/workflows/ai-artifacts.yml
    secrets: inherit

  # --- 将来の安全網（必要になったらコメント解除） ---
  # nightly-full:
  #   name: 🌙 Nightly Full Suite
  #   if: github.event_name == 'schedule'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Placeholder for full E2E/perf/security
  #       run: echo "Run heavy suites here on a schedule"

  # --- デプロイは当面手動に ---
  # deploy:
  #   name: 🚀 Deploy (manual)
  #   needs: [ai-artifacts]
  #   if: ${{ false }}  # 手動に戻す／外す
  #   uses: ./.github/workflows/deploy.yml
  #   secrets: inherit