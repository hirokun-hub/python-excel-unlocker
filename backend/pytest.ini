# This file configures the behavior of pytest for the backend tests.
# It sets a default timeout for all tests to prevent them from hanging,
# and adds the '-q' (quiet) flag to reduce verbosity.

[pytest]
timeout = 20
addopts = -q
