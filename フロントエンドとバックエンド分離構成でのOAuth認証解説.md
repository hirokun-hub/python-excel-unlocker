---
created: "[[2025-07-11]]"
aliases: "分離構成でのOAuth認証フロー"
tags:
  - "認証"
  - "セキュリティ"
  - "Web開発"
  - "OAuth"
  - "フロントエンド"
  - "バックエンド"
---

# フロントエンド・バックエンド分離構成でのOAuth認証解説

フロントエンドとバックエンドを分離した現代的なWebアプリケーションにおいて、[[OAuth 2.0]] は安全で標準的な認証・認可の仕組みを提供します。ここでは、その中でも最も安全性が高いとされる **認可コードフロー (Authorization Code Flow)** が、どのように機能するのかを図解と共に解説します。

## 登場人物の整理

まず、このフローに関わる登場人物を整理します。

- **ユーザー (Resource Owner)**
  - あなたのサービスを利用する人。自分のデータへのアクセスを許可する権限を持ちます。
- **フロントエンド (Client Application)**
  - ユーザーが直接操作するWebアプリケーション。[[Vercel]]などでホスティングされます。（例: [[React]], [[Next.js]]）
  - **ユーザーを認可サーバーへ案内する役割**を持ちます。
- **バックエンド (Resource Server)**
  - パスワード解除処理など、実際の機能を提供するAPIサーバー。[[Render]]などでホスティングされます。（例: [[FastAPI]]）
  - **ユーザーのデータ（リソース）を保護し、フロントエンドからの要求に応じて機能を提供する役割**を持ちます。
- **認可サーバー (Authorization Server)**
  - ユーザーの認証を行い、アクセス許可の証明となる「アクセストークン」を発行する外部サーバー。（例: [[Google]], [[Auth0]], [[Microsoft Entra ID]]）
  - **認証と認可を専門に担当する役割**を持ちます。

## 全体のフロー

全体の流れは以下のようになります。ユーザー、フロントエンド、バックエンド、そして外部の認可サーバーが連携して認証・認可を実現します。

```mermaid
---
title: "分離構成におけるOAuth認可コードフロー"
---
sequenceDiagram
    participant User as ユーザー
    participant Frontend as フロントエンド<br>(Vercel)
    participant Backend as バックエンド<br>(Render)
    participant AuthServer as 認可サーバー<br>(例: Google)

    %% 1. ログイン開始から認可コード取得まで
    User->>+Frontend: 1. ログインボタンをクリック
    Frontend->>User: 2. 認可サーバーのログイン画面へリダイレクト
    User->>+AuthServer: 3. ID/パスワード入力 & アクセス許可
    AuthServer-->>-User: 4. 「認可コード」を付けて<br>フロントエンドへリダイレクト
    User-->>Frontend: 4.1. ブラウザがリダイレクト指示に従う

    %% 2. アクセストークン取得とバックエンドでのログイン処理
    Frontend->>+Backend: 5. 取得した「認可コード」をバックエンドへ送信
    Backend->>+AuthServer: 6. 「認可コード」と<br>Client Secret等を送信
    AuthServer-->>-Backend: 7. 「アクセストークン」と<br>「IDトークン」を発行
    
    Backend->>Backend: 8. トークンを検証し<br>ユーザー情報をDBに保存
    Backend-->>-Frontend: 9. バックエンド独自の<br>セッショントークン(JWT等)を発行

    %% 3. APIの利用
    User->>Frontend: 10. APIを必要とする操作を行う
    Frontend->>+Backend: 11. セッショントークンを付けて<br>APIリクエスト
    Backend-->>-Frontend: 12. 認証OK、APIの応答を返す
```

## 各ステップの詳細解説

### ステップ1〜4: 認可コードの取得

これは、ユーザーに「これからこのアプリがあなたの情報（名前やメールアドレスなど）を使いますがいいですか？」と許可をもらうためのフェーズです。

1.  **ログイン開始**: ユーザーがフロントエンドの「Googleでログイン」などのボタンを押します。
2.  **リダイレクト**: フロントエンドは、ユーザーを認可サーバーのログインページに送り出します（リダイレクト）。このとき、「ログインが終わったらここに戻ってきてください」という情報（リダイレクトURI）などをURLに含めます。
3.  **認証と同意**: ユーザーは認可サーバー（例: Google）の画面でIDとパスワードを入力してログインし、「このアプリケーションに情報を提供することを許可しますか？」という画面で「許可」ボタンを押します。
4.  **認可コードの発行**: 認可サーバーは、ユーザーの許可が出た証として、一時的な **認可コード** を発行します。そして、ブラウザをステップ2で指定されたフロントエンドのURLにリダイレクトさせます。このとき、URLのパラメータとして認可コードが付与されます。

> **ポイント**
> この段階では、まだアプリケーションはユーザーが誰であるかを確定できていません。あくまで「一時的な許可証（認可コード）」を受け取っただけです。
> フロントエンドはユーザーを認可サーバーに案内するだけで、ユーザーのパスワードには一切触れないため、安全です。

### ステップ5〜9: アクセストークンの取得とログインセッションの確立

これは、フロントエンドが受け取った「認可コード」を使い、バックエンドが正規の「通行証（アクセストークン）」を取得して、アプリケーション独自のログイン状態を作り出すフェーズです。

5.  **認可コードの送信**: フロントエンドは、URLから受け取った認可コードをバックエンドに送信します。
6.  **トークンの要求**: バックエンドは、受け取った認可コードに、事前に認可サーバーに登録しておいた自身の身元情報（クライアントID, クライアントシークレット）を添えて、認可サーバーに送信します。
7.  **トークンの発行**: 認可サーバーは、情報が正しければ **アクセストークン** と **IDトークン** をバックエンドに発行します。
    -   **アクセストークン**: バックエンドがユーザーの代わりに、保護されたリソース（例: Googleカレンダーの情報など）にアクセスするための鍵。
    -   **IDトークン**: ユーザーが誰であるか（名前、メールアドレスなど）を証明する身分証明書のようなもの（[[JWT]]形式）。
8.  **ユーザー情報の保存**: バックエンドはIDトークンを検証してユーザーの身元を確認し、その情報を自身のデータベースに保存（または更新）します。
9.  **セッション確立**: バックエンドは、自身のサービス内でのログイン状態を示すための独自のトークン（これも[[JWT]]であることが多い）を生成し、フロントエンドに返します。

> **ポイント**
> 最も重要な **アクセストークン** や、漏洩してはならない **クライアントシークレット** は、サーバー間通信（バックエンド ↔ 認可サーバー）でのみやり取りされます。ブラウザ（フロントエンド）には一切渡らないため、非常に安全です。これを **Backend for Frontend (BFF)** パターンと呼ぶこともあります。

### ステップ10〜12: APIの利用

一度ログインセッションが確立すれば、あとは簡単です。

10. **操作実行**: ユーザーが、認証が必要なAPI機能（例: Excelファイルのアップロード）をフロントエンドで操作します。
11. **APIリクエスト**: フロントエンドは、ステップ9でバックエンドから受け取ったセッショントークンをリクエストヘッダーに含めて、バックエンドのAPIを呼び出します。
12. **処理実行と応答**: バックエンドは、リクエストヘッダーのセッショントークンを検証し、有効であれば要求された処理を実行して結果をフロントエンドに返します。

## まとめ

この構成のメリットは以下の通りです。

- **高いセキュリティ**: ユーザーのパスワードや、重要なアクセストークンがフロントエンド（ブラウザ）に漏れることがありません。
- **責務の分離**: フロントエンドはUI、バックエンドはビジネスロジック、認可サーバーは認証・認可と、それぞれの役割が明確に分かれているため、メンテナンス性や拡張性が高まります。
- **標準的な実装**: OAuth 2.0は業界標準であるため、多くのライブラリやサービスが対応しており、一度理解すれば他のサービスにも応用が効きます。

ご希望のシステム構成において、この認証フローは最適な選択肢の一つと言えるでしょう。 