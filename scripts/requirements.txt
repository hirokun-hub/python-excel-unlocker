# GitHub Secrets環境変数設定に必要な最小限のパッケージ
# 軽量化とセキュリティを重視した構成

# JSON Schema検証（設定ファイル検証用）
jsonschema>=4.0.0,<5.0.0

# HTTP リクエスト（GitHub API連携用）
requests>=2.25.0,<3.0.0

# 暗号化（オプション機能用）
cryptography>=3.4.0,<42.0.0

# YAML処理（設定ファイル用、オプション）
PyYAML>=6.0,<7.0

# 日付・時刻処理（ログ用）
python-dateutil>=2.8.0,<3.0.0